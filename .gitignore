# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride

# --- Editors / IDE ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- Env / secrets ---
.env
.env.*
!.env.example

# --- Python (primary language per implementation plan) ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.Python
build/
dist/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
.installed.cfg
MANIFEST

# Virtualenvs
.venv/
venv/
env/
ENV/
.python-version

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
.hypothesis/
nosetests.xml
coverage.xml

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# uv / poetry / pdm lockfile artifacts (keep lockfiles, ignore caches)
.pdm-python
.pdm-build/

# --- Node (if a UI/TS layer is added later) ---
node_modules/
dist/
.next/
.turbo/

# --- Project: research agent runtime ---
# SQLite indexes, llm cache, checkpoints — large, machine-local, regenerable
data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.db

# Per-job outputs (markdown + json). Large + machine-generated.
# Promote anything publishable into a curated dir and `git add -f` if needed.
jobs/
runs/

# Local research corpus — large PDFs/notes; keep the dir, ignore contents.
corpus/*
!corpus/.gitkeep

# Event logs / structured logs
logs/
*.log
*.jsonl

# Canonical per-job contract fixtures are intentionally checked in.
!tests/fixtures/jobs/
!tests/fixtures/jobs/**

# Sessions / scratch state
sessions/
.cache/
tmp/
.tmp/

# --- alpha-loop ---
# Ignore everything under .alpha-loop/ except committed templates (skills + agent prompts).
.alpha-loop/*
!.alpha-loop/templates/

# --- Playwright ---
.playwright/
.playwright-cli/
playwright-report/
test-results/
playwright/.auth/

# --- Models / large artifacts (don't accidentally commit local LLM files) ---
*.gguf
*.bin
*.safetensors
*.pt
*.pth
models/

# --- OS / misc ---
*.pid
*.seed
# Alpha-loop ephemeral data (not shared)
.alpha-loop/sessions/
.alpha-loop/auth/
.alpha-loop/templates/*.bak
.worktrees/

# --- added by checkin-all.sh ---
*.mov
*.MOV
*.mp4
*.m4a
*.mp3
*.wav
*.psd
*.gguf
*.safetensors
inputs/
outputs/
exports/
media/
previews/
renders/
_kin/
.venv/
venv/
node_modules/
__pycache__/
.worktrees/
dist/
build/
.next/
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.key

rippling_transactions_export_*.zip
